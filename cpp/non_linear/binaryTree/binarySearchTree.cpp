#include <'./include/TreeNode.h'>
#include <iostream>

class BST {
  private:
    TreeNode *root;

  public:
    BST() : root(nullptr) {}
    
    void add(int x) {
      TreeNode *node = new TreeNode(x):

      // empty tree
      if (root == nullptr) root = node;
      else {
        if (x > )
      }
    }
};